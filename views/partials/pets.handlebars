{{!-- <div class="card mb-1">
   <div class="card-header card-title">
      {{petname}}
   </div>
   <div class="card-body card-body-text">
      <br>

      {{pet_info}}

      <br>
      <br>
      <div class="post-comments-info float-end">
         <span class="plain-text">By</span> <span class="info-text">{{user.username}}</span> <span
            class="plain-text">on</span> <span class="info-text">{{format_date created_at}}</span>
      </div>
   </div>
</div> --}}